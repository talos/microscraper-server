<div class="test ui-widget-content">
  <div class="code">
    JSON (<a href="{{location}}?format=json">link</a>):<br>
    <code class="json">
      {{to_json}}
    </code>
  </div>
  <form class="test" method="get">
    <table class="variables">
      {{#variables}}
        <tr>
          <td><label for="{{to_s}}">{{to_s}}:</label></td>
          <td><textarea name="{{to_s}}" class="disabler"></textarea></td>
        </tr>
      {{/variables}}
    </table>
  </form>
</div>

