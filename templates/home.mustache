{{#html_format}}
  {{#user}}
    <div class="home ui-widget">
      Your contributions:
      {{> associations}}
      {{#options}}
      <div class="modify ui-widget-content">
        <form method="put" action="{{home_location}}">
          <input name="title" type="text" />
          <button type="submit">Change Name</button>
        </form>
      </div>
      {{/options}}
    </div>
  {{/user}}
{{/html_format}}
